#include <unistd.h>
#include "main.h"

/**
 * mul - multiply 2 numbers
 *@a: The character to input
 *@b: the character to input
 *
 * Return: 0 indicate success
 */

int mul(int a, int b)
{
	int i = a;
	int j = b;
	int result = i * j;

	return (result);
}
